#include <stdio.h>

int main() {
    int a = 10;
    float b = 20.5;
    if (a < b) {
        printf("a is less than b\n");
    } else {
        printf("a is not less than b\n");
    }
    return 0;
}
